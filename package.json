{
  "name": "pp5-grading-system",
  "version": "0.1.0",
  "private": true,
  "description": "ระบบ ปพ.5 (PP.5 School Grading System) - Monorepo root",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "dev:admin": "pnpm --filter @pp5/admin dev",
    "dev:parent": "pnpm --filter @pp5/parent dev",
    "db:types": "supabase gen types typescript --project-id tizntrnmvctllwylhthl --schema public > packages/database/src/types.ts"
  },
  "devDependencies": {
    "supabase": "^1.226.4",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@11.1.0",
  "engines": {
    "node": ">=20"
  }
}
